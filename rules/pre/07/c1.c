#include <stdio.h>
#include <stddef.h>
#include <stdint.h>

int main(){
size_t i /* Some initial value */;
/* Assignment of i */
if (i > 9000) {
   if (puts("Over 9000!?""?!") == EOF) {
     /* Handle error */
   }
}
}